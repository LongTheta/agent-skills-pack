# Shell — Invocation Template

The shell skill is invoked explicitly via `/shell`. No template needed—the text after `/shell` is executed literally.

```
/shell [command]
```

Examples:
- `/shell npm install`
- `/shell git status`
- `/shell python -m pytest tests/`
